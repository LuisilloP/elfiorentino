---
import Button from "../button/Button.astro";
import WhatsAppBtn from "../button/WhatsAppBtn.astro";
import Container from "../layout/Container.astro";

const trustPoints = [
  "Diseño y fabricación a medida",
  "Instalación coordinada en la IV Región",
  "Garantía y acompañamiento posventa",
];
---

<section class="section-shell">
  <Container>
    <div
      class="relative overflow-hidden surface-card card-bleed bg-ink text-white"
    >
      <picture class="absolute inset-0 z-0">
        <source srcset="/indexImage/Banner.jpg" type="image/jpeg" />
        <img
          src="/indexImage/Banner.jpg"
          alt=""
          aria-hidden="true"
          class="h-full w-full object-cover opacity-35"
          loading="lazy"
          decoding="async"
          width="1600"
          height="720"
        />
      </picture>
      <div
        class="absolute inset-0 z-10 bg-gradient-to-r from-black/80 via-black/60 to-transparent"
      >
      </div>

      <div
        class="relative z-20 flex flex-col items-center gap-6 px-6 py-12 text-center md:px-10 md:py-16"
      >
        <p
          class="text-sm uppercase tracking-[0.28em] text-accent reveal-on-scroll"
        >
          A medida para ti
        </p>
        <h2
          class="text-heading text-3xl md:text-4xl font-semibold leading-[1.1] reveal-on-scroll reveal-delay-100"
        >
          Si no encontraste lo que necesitas, contáctanos
        </h2>
        <p
          class="max-w-2xl text-base md:text-lg text-white/80 reveal-on-scroll reveal-delay-200"
        >
          Cuéntanos tu idea y la diseñamos juntos. Desde la selección de
          materiales hasta la instalación, te acompañamos en cada paso.
        </p>
        <div
          class="flex flex-col gap-3 sm:flex-row sm:items-center reveal-on-scroll reveal-delay-300"
        >
          <WhatsAppBtn textBtn="Quiero cotizar" />
          <Button
            label="Ver catálogo"
            href="#catalogo"
            variant="outline"
            class="text-white border-white/60 hover:border-white hover:bg-white/10"
          />
        </div>

        <ul
          class="grid w-full max-w-4xl grid-cols-1 gap-3 text-left sm:grid-cols-3 reveal-on-scroll reveal-delay-400"
        >
          {
            trustPoints.map((point) => (
              <li class="flex items-center items-start gap-3 rounded-lg border border-white/15 bg-white/5 p-3 backdrop-blur-sm">
                <span
                  class="mt-1 inline-flex h-6 w-6 items-center justify-center rounded-full bg-white/15 text-sm font-semibold text-accent"
                  aria-hidden="true"
                >
                  <svg
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.6"
                    class="h-4 w-4"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      d="m5 13 4 4L19 7"
                    />
                  </svg>
                </span>
                <p class="text-sm text-white/90">{point}</p>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </Container>
</section>
